public boolean endsLy(String str) {
  if(str.length() < 2){
    return false;
  }
  String str1 = str.substring(str.length()-2,str.length());
  if(str1.equals("ly")){
    return true;
  }
  return false;
}
