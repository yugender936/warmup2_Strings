public String nTwice(String str, int n) {
  String str1 = "";
  for(int i = 0 ; i < n ; i++){
    str1 = str1 + str.charAt(i);
  }
  int k = str.length() - n;
  while(n > 0){
    str1 = str1 + str.charAt(k);
    k++;
    n--;
  }
  return str1;
}
