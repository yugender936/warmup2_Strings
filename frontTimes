public String frontTimes(String str, int n) {
  String str1 = "";
  if(str.length() < 3){
    for(int i = 1 ; i <= n ; i++){
      str1 = str1 + str;
    }
  }
  else{
    String str2 = str.substring(0,3);
    for(int i = 0 ; i < n ; i++){
      str1 = str1 + str2;
    }
  }
  return str1;
}
