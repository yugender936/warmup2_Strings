public String lastTwo(String str) {
  if(str.length() < 2){
    return str;
  }
  String str1 = ""+str.charAt(str.length()-1)+str.charAt(str.length()-2);
  String str2 = str.substring(0,str.length()-2)+str1;
  return str2;
}
